{% extends 'base.html.twig' %}

{% block title %}Warehouse Index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Warehouses</h1>
        <a href="{{ path('app_warehouse_new') }}" class="btn btn-primary">Create new</a>
    </div>

    <table class="table table-striped table-hover align-middle">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Location</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for warehouse in warehouses %}
            <tr>
                <td>{{ warehouse.id }}</td>
                <td>{{ warehouse.name }}</td>
                <td>{{ warehouse.location }}</td>
                <td>
                    <a href="{{ path('app_warehouse_edit', {'id': warehouse.id}) }}" class="btn btn-sm btn-outline-primary">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" class="text-center py-3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
